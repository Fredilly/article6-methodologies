# Normalize text files
* text=auto eol=lf

# PDFs via Git LFS (except shared source archives / previous-version tool sources)
source-assets/**/*.pdf -filter -diff -merge -text
tools/**/previous/**/tools/*.pdf -filter -diff -merge -text
*.pdf filter=lfs diff=lfs merge=lfs -text

# Vendored tarballs: keep binary; never LFS; no EOL munging
vendor/** -text
*.tgz binary -filter -diff -merge
