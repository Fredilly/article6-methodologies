# Normalize text files
* text=auto eol=lf

# PDFs via Git LFS
*.pdf filter=lfs diff=lfs merge=lfs -text

# Vendored tarballs: keep binary; never LFS; no EOL munging
vendor/** -text
*.tgz binary -filter -diff -merge
