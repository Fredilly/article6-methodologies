#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if [ -x "$ROOT/scripts/validators/ingest.sh" ]; then
  exec "$ROOT/scripts/validators/ingest.sh" "$@"
fi
exec "$ROOT/scripts/ingest.sh" "$@"
