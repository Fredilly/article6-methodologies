# RC-20260507-135214 — Previous-version META audit hash drift (superseded stub)
- Date: 2026-05-07
- Area: CI / previous / META
Tags: [ci, determinism, previous, agriculture, forestry]

## Symptom
An earlier `npm run root-cause:new` invocation created a partial record with redacted placeholders after shell quoting stripped literal field names from the prompt arguments.

## Impact
The malformed entry would keep the root-cause index technically valid but materially misleading.

## Root cause
The command invocation embedded shell-sensitive quoting, which allowed argument parsing to drop literal identifiers before the template renderer wrote the file.

## Fix
Supersede this stub with [RC-20260507-135220](RC-20260507-135220.md), which records the same incident with complete field values and proof references.

## New invariants / guardrails
When creating root-cause entries from the shell, avoid backticks and other shell-sensitive quoting inside freeform argument values.

## Proof / tests
`git diff` of this file versus the malformed original shows placeholder-only content replaced by a truthful supersession note.

## Follow-ups
- [ ] Keep `RC-20260507-135220` as the canonical incident record for this failure.
